export type TodoStatus = "pendente" | "em andamento" | "realizada"; export type ActiveTodoStatus = Exclude<TodoStatus, "realizada">; /** * Registro de tarefa armazenado na tabela `todos` do Supabase. */ export interface Todo { id: string; user_id: string; title: string; completed: boolean; status: TodoStatus; created_at: string; start_date: string; // Nova coluna para data/hora de início deadline_date: string; // Nova coluna para data/hora do prazo final }